# 📁 Руководство по миграции структуры проекта

Проект реорганизован в 3 отдельных приложения:

```
ilialox/
├── backend/          ✅ Бэкенд API (Node.js + Express + Prisma)
├── public-site/      📝 Публичный сайт (Landing + TrackRequest)
└── crm/              📝 CRM система (Login + Dashboard + Заявки)
```

## Шаг 1: Запуск скрипта миграции

Откройте PowerShell в корне проекта и выполните:

```powershell
.\migrate-structure.ps1
```

Скрипт автоматически скопирует все файлы в нужные папки.

## Шаг 2: Установка зависимостей

### Public Site (порт 3000)
```bash
cd public-site
npm install
```

### CRM (порт 5173)
```bash
cd crm
npm install
```

### Backend (порт 5000) - уже установлено
```bash
cd backend
# Зависимости уже установлены
```

## Шаг 3: Запуск приложений

### Запуск бэкенда
```bash
cd backend
npm run dev
```
Сервер запустится на http://localhost:5000

### Запуск публичного сайта
```bash
cd public-site
npm run dev
```
Сайт откроется на http://localhost:3000

### Запуск CRM
```bash
cd crm
npm run dev
```
CRM откроется на http://localhost:5173

## Шаг 4: Проверка работы

1. **Public Site (http://localhost:3000)**
   - Откройте главную страницу
   - Заполните форму заявки
   - Проверьте страницу отслеживания `/track`

2. **CRM (http://localhost:5173)**
   - Войдите с логином: `admin@ilialox.com` / `admin123`
   - Проверьте Dashboard
   - Откройте список заявок
   - Просмотрите детали заявки

3. **Backend**
   - Проверьте логи в консоли бэкенда
   - Убедитесь что нет ошибок подключения к БД

## Шаг 5: Очистка старой структуры

❗ **Только после того, как всё заработало!**

```bash
# Удалите старую папку src и конфигурации в корне
rm -rf src
rm package.json package-lock.json vite.config.ts index.html .env node_modules
```

## Структура портов

- **3000** - Public Site (Публичный сайт)
- **5000** - Backend API
- **5173** - CRM (Панель управления)

## Полезные команды

### Сборка для production

```bash
# Public Site
cd public-site && npm run build

# CRM
cd crm && npm run build

# Backend
cd backend && npm run start
```

### Одновременный запуск всех сервисов

Создайте файл `start-all.ps1` в корне:

```powershell
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd backend; npm run dev"
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd public-site; npm run dev"
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd crm; npm run dev"
```

Затем запустите:
```powershell
.\start-all.ps1
```

## Возможные проблемы

### Порт уже занят
Если порт занят, измените в `vite.config.ts`:
```ts
server: {
  port: 3001, // измените порт
}
```

### Ошибки импортов
Убедитесь что все пути импортов используют относительные пути или алиас `@/`:
```ts
import { Component } from '@/components/Component'
```

### Ошибка "Cannot find module"
Запустите:
```bash
npm install
```

## Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи сервера в терминале
3. Убедитесь что все зависимости установлены
4. Проверьте файл `.env` в каждом проекте
