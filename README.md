# Ilialox Auto Service CRM - 2-Day Prototype

Полнофункциональный прототип CRM-системы для автосервиса с публичной частью (landing page) и административной панелью.

## 🚀 Быстрый старт

### Требования

- Node.js 20+
- PostgreSQL 15+
- npm или yarn

### Установка и запуск

#### 1. Backend (API Server)

```bash
# Перейти в папку backend
cd backend

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env
# Отредактируйте .env и укажите ваши настройки DATABASE_URL

# Создать базу данных PostgreSQL
createdb ilialox_crm

# Запустить миграции
npx prisma migrate dev --name init

# Сгенерировать Prisma Client
npx prisma generate

# Создать админ пользователя и тестовые данные
npm run seed

# Запустить сервер
npm run dev
```

Backend будет доступен на `http://localhost:5000`

#### 2. Frontend (React + Vite)

```bash
# Вернуться в корень проекта
cd ..

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env

# Запустить фронтенд
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

## 🔑 Данные для входа

**CRM Admin:**
- Email: `admin@ilialox.com`
- Пароль: `admin123`

## 📁 Структура проекта

```
ilialox/
├── backend/                    # Node.js + Express API
│   ├── src/
│   │   ├── config/            # Prisma client
│   │   ├── controllers/       # Request handlers
│   │   ├── middleware/        # Auth, error handling
│   │   ├── routes/            # API routes
│   │   ├── services/          # Business logic
│   │   ├── utils/             # Helpers, constants
│   │   └── server.js          # Express app
│   ├── prisma/
│   │   └── schema.prisma      # Database schema
│   └── package.json
│
├── src/                        # React Frontend
│   ├── components/
│   │   ├── crm/               # CRM components
│   │   ├── shared/            # Shared components
│   │   └── ...
│   ├── contexts/              # React contexts (Auth)
│   ├── pages/                 # Page components
│   │   ├── Landing.tsx        # Public landing page
│   │   ├── TrackRequest.tsx   # Public tracking
│   │   ├── Login.tsx          # CRM login
│   │   ├── Dashboard.tsx      # CRM dashboard
│   │   ├── RequestsList.tsx   # Requests list
│   │   └── RequestDetails.tsx # Request details
│   ├── services/              # API client
│   ├── types/                 # TypeScript types
│   ├── utils/                 # Constants, helpers
│   └── App.tsx                # Router config
│
├── 2-DAY-PROTOTYPE-PLAN.md    # Development plan
├── QUICK_START.md             # Setup guide
└── package.json
```

## 🎯 Возможности прототипа

### Публичная часть (Landing Page)

✅ **Лендинг с информацией о сервисе**
- Hero секция с ключевой информацией
- Описание услуг
- Галерея работ
- Отзывы клиентов
- Контакты

✅ **Форма подачи заявки**
- Информация о клиенте (имя, телефон, email)
- Данные автомобиля (марка, модель, год, гос. номер)
- Описание проблемы
- Автоматическое создание заявки в системе
- Получение номера заявки и ссылки для отслеживания

✅ **Отслеживание заявки**
- Поиск по номеру заявки или прямая ссылка
- Просмотр текущего статуса
- Прогресс выполнения (%)
- Информация о назначенном мастере
- Список выполненных работ
- Общая сумма и статус оплаты
- История изменения статусов

### CRM (Административная панель)

✅ **Аутентификация**
- Вход по email/пароль
- JWT токены
- Защищенные маршруты

✅ **Dashboard (Панель управления)**
- Общая статистика (всего заявок, новых, в работе, завершенных)
- Выручка за сегодня
- Заявки, ожидающие оплаты
- Список последних заявок

✅ **Управление заявками**
- Список всех заявок с фильтрацией
  - Фильтр по статусу (Все / Новые / В работе / Завершенные / Отмененные)
  - Поиск по номеру заявки или имени клиента
- Просмотр деталей заявки
  - Полная информация о клиенте и автомобиле
  - Описание проблемы
  - Изменение статуса
  - Назначение мастера (выбор из списка)
  - Обновление прогресса выполнения (0-100%)
  - Добавление работ/услуг с ценами
  - Автоматический расчет общей суммы
  - Изменение статуса оплаты
  - История изменений статусов
  - Ссылка для отслеживания клиентом

## 🛠 Технологии

### Backend
- **Runtime:** Node.js 20
- **Framework:** Express.js
- **Database:** PostgreSQL 15
- **ORM:** Prisma
- **Auth:** JWT (jsonwebtoken)
- **Validation:** express-validator

### Frontend
- **Framework:** React 18
- **Build tool:** Vite
- **Routing:** React Router v6
- **HTTP client:** Axios
- **Styling:** TailwindCSS
- **UI Components:** Radix UI, Lucide React
- **Forms:** React Hook Form + Zod
- **State:** React Context API

## 📊 База данных

Прототип использует упрощенную схему из 6 таблиц:

1. **users** - Пользователи CRM
2. **clients** - Клиенты (создаются автоматически при подаче заявки)
3. **vehicles** - Автомобили
4. **service_requests** - Заявки на обслуживание
5. **request_works** - Работы/услуги по заявке
6. **request_status_history** - История изменения статусов

## 🔄 API Endpoints

### Public (без авторизации)
- `POST /api/public/requests` - Создать заявку
- `GET /api/public/track/:token` - Отследить по токену
- `GET /api/public/track/number/:requestNumber` - Отследить по номеру

### Auth
- `POST /api/auth/login` - Вход в CRM

### Dashboard (защищенные)
- `GET /api/dashboard/stats` - Статистика

### Requests (защищенные)
- `GET /api/requests` - Список заявок (с фильтрами)
- `GET /api/requests/:id` - Детали заявки
- `PATCH /api/requests/:id/status` - Изменить статус
- `PATCH /api/requests/:id/assign` - Назначить мастера
- `PATCH /api/requests/:id/progress` - Обновить прогресс
- `POST /api/requests/:id/works` - Добавить работу
- `DELETE /api/requests/:id/works/:workId` - Удалить работу
- `PATCH /api/requests/:id/payment` - Обновить статус оплаты

## 🧪 Тестирование

1. **Публичный flow:**
   - Откройте `http://localhost:5173`
   - Заполните форму заявки
   - Получите номер заявки
   - Перейдите по ссылке отслеживания

2. **CRM flow:**
   - Откройте `http://localhost:5173/login`
   - Войдите (admin@ilialox.com / admin123)
   - Просмотрите dashboard
   - Откройте список заявок
   - Обновите статус, назначьте мастера
   - Добавьте работы и услуги

## 📝 Что упрощено в прототипе

⚠️ Для экономии времени следующие модули упрощены или мокированы:

- **Мастера** - Хардкод список (нет CRUD)
- **Услуги** - Предопределенный список
- **Клиенты/Автомобили** - Создаются автоматически, нет отдельного управления
- **Файлы** - Загрузка не реализована
- **Уведомления** - Не реализованы
- **Интеграции** - Telegram/WhatsApp/SMS не подключены
- **Аналитика** - Только базовая статистика, без графиков
- **Мультиюзер** - Один админ, нет ролей

## 🚀 Следующие шаги

После успешного прототипа можно продолжить разработку согласно `DEVELOPMENT_PLAN.md`:

1. Sprint 1-2: Полное управление клиентами и автомобилями
2. Sprint 3-4: CRUD для мастеров с расписаниями
3. Sprint 5-6: Складской учет запчастей
4. Sprint 7-8: Платежи и финансы
5. Sprint 9-10: Документы и файлы
6. Sprint 11-12: Аналитика с графиками
7. Sprint 13+: Интеграции (Telegram, WhatsApp, SMS)

## 📖 Документация

Полная документация доступна в файлах:

- `TECHNICAL_SPECIFICATION.md` - Техническое задание по модулям
- `DATABASE_SCHEMA.md` - Полная схема БД (25 таблиц)
- `API_DOCUMENTATION.md` - Спецификация API (100+ endpoints)
- `PROJECT_STRUCTURE.md` - Архитектура проекта
- `DEVELOPMENT_PLAN.md` - План разработки (12 спринтов)

## 🐛 Troubleshooting

### Backend не запускается
- Проверьте что PostgreSQL запущен
- Проверьте DATABASE_URL в `.env`
- Запустите `npx prisma generate`

### Frontend ошибки API
- Проверьте что backend запущен на порту 5000
- Проверьте VITE_API_URL в `.env`

### Ошибка при логине
- Убедитесь что запустили `npm run seed`
- Проверьте JWT_SECRET в backend `.env`

## 📞 Поддержка

Для вопросов и проблем создайте issue в репозитории.

---

**Разработано за 2 дня** ⚡️
